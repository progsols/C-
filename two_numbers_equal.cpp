//Write a program that inputs two numbers
//and finds out whether both numbers are equal.

#include<iostream.h>
#include<conio.h>

void main()
{

	int a, b;
	clrscr();
	cout<<"Enter first number ? ";
	cin>>a;
	cout<<"Enter second number ? ";
	cin>>b;
	if(a == b)
		cout<<"Both numbers are equal"<<endl;
	cout<<"End of the program";
	getch();	
}
